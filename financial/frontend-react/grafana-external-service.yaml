apiVersion: v1
kind: Service
metadata:
  name: grafana-external
  namespace: financial
spec:
  type: ExternalName
  externalName: grafana.grafana.svc.cluster.local
  ports:
    - port: 80
      targetPort: 3000