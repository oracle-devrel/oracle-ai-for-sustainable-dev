# Local Spring Boot app settings for observability/run-app.sh.
# Copy this file to .env and fill in values for your database.

DB_URL=jdbc:oracle:thin:@//127.0.0.1:1521/FREEPDB1
# Created by sql/setup_local_deep_data_security.sql. FINANCIAL remains the
# schema owner; the app logs in directly as this local Deep Sec END USER.
DB_USERNAME='"claims-investigator-agent"'
DB_PASSWORD=replace-with-local-deep-sec-end-user-password

# App/JDBC spans can export to a local Jaeger OTLP HTTP endpoint.
OTLP_TRACES_ENDPOINT=http://localhost:4318/v1/traces
TRACE_SAMPLE_PROBABILITY=1.0
OTEL_SEMCONV_STABILITY_OPT_IN=database/dup

# Server-side trace propagation toggles used by the demo app.
ORACLE_JDBC_SERVER_TELEMETRY_TRACES_ENABLED=true
ORACLE_JDBC_SERVER_TELEMETRY_LOGGING_ENABLED=false
ORACLE_JDBC_TRACEPARENT_CLIENT_INFO_ENABLED=true
ORACLE_JDBC_TRACELEVEL_CLIENT_INFO_ENABLED=true
